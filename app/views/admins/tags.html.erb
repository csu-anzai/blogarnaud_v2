tags.html.erb
<!-- top menu -->
<%= render 'menu-admin' %>

<!-- add spacer for menu -->
<div class="top-menu-spacer"></div>

<!-- administrate entreprises  -->
<main class="container-main">

  <h1><i class="fas fa-user-lock"></i> Administration des tags</h1>

  <!-- button to create and article -->
  <button class="button-admin">
    <%= link_to "Créer un tag", new_tag_path %>
  </button>

  <!-- iterate through all entreprises and create a card for each of them -->
  <% @tags.each do |tag| %>
    <%= image_tag(tag.icon) %>
    <%= tag.name_fr %>
    <%= tag.name_en %>
  <% end %>

</main>
